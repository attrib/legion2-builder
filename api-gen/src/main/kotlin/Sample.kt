/*
fun main(args: Array<String>) {
    LegionData.legions.filter { it.playable }.forEach {
        println(it.name)
    }
    LegionData.units.forEach {
        println("${it.name} ${LegionData.legionsMap[it.legion]!!.name}")
    }
    println(LegionData.unitsMap["atom_unit_id"])
}
*/
